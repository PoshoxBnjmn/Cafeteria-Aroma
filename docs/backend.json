{
  "entities": {
    "Product": {
      "title": "Product",
      "description": "Represents a product in the cafeteria's menu.",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the product." },
        "name": { "type": "string", "description": "The name of the product." },
        "description": { "type": "string", "description": "A brief description of the product." },
        "price": { "type": "number", "description": "The price of the product. Can be 0 for redeemable items." },
        "category": {
          "type": "string",
          "enum": ["Bebestibles Calientes", "Frappes / Bebidas Frías", "Dulces", "Salados", "Canjeables"],
          "description": "The category the product belongs to."
        },
        "image": { "type": "string", "format": "uri", "description": "URL of the product image." },
        "imageHint": { "type": "string", "description": "A hint for AI image generation or search." },
        "points": { "type": "number", "description": "Points awarded to the user upon purchasing this product." },
        "pointsCost": { "type": "number", "description": "Points required to redeem this product." }
      },
      "required": ["id", "name", "price", "category", "image"]
    },
    "User": {
      "title": "User",
      "description": "Represents a registered user of the application.",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the user." },
        "name": { "type": "string", "description": "The user's display name." },
        "email": { "type": "string", "format": "email", "description": "The user's email address." },
        "profilePicture": { "type": "string", "format": "uri", "description": "URL of the user's profile picture." },
        "points": { "type": "number", "description": "The user's current loyalty points balance." },
        "role": {
          "type": "string",
          "enum": ["admin"],
          "description": "The user's role, which can grant special permissions."
        }
      },
      "required": ["id", "name", "email", "points"]
    },
    "Order": {
      "title": "Order",
      "description": "Represents an order placed by a user.",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the order." },
        "userId": { "type": "string", "description": "The ID of the user who placed the order." },
        "date": { "type": "string", "format": "date-time", "description": "The date and time the order was placed." },
        "items": {
          "type": "array",
          "description": "List of items included in the order.",
          "items": {
            "type": "object",
            "properties": {
              "productId": { "type": "string" },
              "productName": { "type": "string" },
              "quantity": { "type": "number" },
              "price": { "type": "number" },
              "pointsAwarded": { "type": "number" }
            },
            "required": ["productId", "productName", "quantity", "price"]
          }
        },
        "total": { "type": "number", "description": "The total cost of the order." },
        "totalPointsAwarded": { "type": "number", "description": "Total points awarded for this order." }
      },
      "required": ["id", "userId", "date", "items", "total", "totalPointsAwarded"]
    },
    "Testimonial": {
      "title": "Testimonial",
      "description": "Represents a customer testimonial.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "quote": { "type": "string" },
        "image": { "type": "string", "format": "uri" },
        "imageHint": { "type": "string" }
      },
      "required": ["id", "name", "quote", "image"]
    },
    "PointsHistoryEntry": {
        "title": "Points History Entry",
        "description": "Represents a single transaction in a user's points history.",
        "type": "object",
        "properties": {
            "id": { "type": "string" },
            "userId": { "type": "string" },
            "date": { "type": "string", "format": "date-time" },
            "description": { "type": "string" },
            "amount": { "type": "number", "description": "Positive for earnings, negative for spending." }
        },
        "required": ["id", "userId", "date", "description", "amount"]
    }
  },
  "auth": {
    "providers": ["email"]
  }
}
